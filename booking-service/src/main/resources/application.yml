server:
  port: 8082

spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:postgresql://localhost:5432/booking_db
    username: booking
    password: booking
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

management:
  endpoint:
    health:
      show-details: never
