server:
  port: 8084

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: appuser
    password: appuser
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

management:
  endpoint:
    health:
      show-details: never
