server:
  port: 8085

spring:
  application:
    name: notification-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.ticketbooking.common.event
  # Real SMTP, not a stub — Gmail App Password. Credentials via env vars
  # GMAIL_USERNAME / GMAIL_APP_PASSWORD, never committed to git.
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME:}
    password: ${GMAIL_APP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoint:
    health:
      show-details: never
  health:
    # Actuator's default mail indicator opens a real SMTP connection on every
    # /actuator/health call, which Gmail throttles under repeated polling
    # ("454 4.7.0 Too many login attempts"). Email sending itself
    # (JavaMailSender.send) is unaffected — this only disables the noisy
    # per-poll connection check, not real delivery.
    mail:
      enabled: false

user-service:
  base-url: http://localhost:8084

event-service:
  base-url: http://localhost:8081

payment-service:
  base-url: http://localhost:8083
